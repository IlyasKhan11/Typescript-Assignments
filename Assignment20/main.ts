const list:String[]=['Japan','China','Brazil','Mexico','Canada','Pakistan'];

// FOllowing line of code prints the list of the given Array 

list.forEach((item,i)=>{
    console.log(`${i}:${item} \n`)
})