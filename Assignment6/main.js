// Following will be the name used 
var persons_name = "\tilyas khan\n";
// following line of code will do the first part of the task, adding whitespaces to the begining and end 
console.log("the persons name with white spaces in the end and start would be: ".concat(persons_name));
// Follwing line of code willl strip the name string 
console.log(" The persons name after stripping the string would be:".concat(persons_name.trim()));
