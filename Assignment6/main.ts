// Following will be the name used 
const persons_name:string="\tilyas khan\n"

// following line of code will do the first part of the task, adding whitespaces to the begining and end 
console.log(`the persons name with white spaces in the end and start would be: ${persons_name}`)

// Follwing line of code willl strip the name string 
console.log(` The persons name after stripping the string would be:${persons_name.trim()}`)